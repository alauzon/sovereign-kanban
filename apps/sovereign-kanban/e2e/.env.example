# Copy to e2e/.env and fill in. NEVER commit e2e/.env (it holds a password).
# Use a DEDICATED test account without 2FA. Prefer an app-password if the login
# form accepts it on your instance; otherwise the account's login password.
NC_TEST_URL=https://cloud.entretablees.ca
NC_TEST_USER=
NC_TEST_PASSWORD=
NC_TEST_BOARD=bienvenue
